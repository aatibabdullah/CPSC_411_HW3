<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.homeworkthree">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Homework3"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
    <activity
        android:name=".SummaryDetailActivity"
        android:parentActivityName=".SummaryLVActivity"
        />
        <activity android:name=".AddStudentActivity"
            android:parentActivityName=".SummaryLVActivity"
            />
        <activity android:name=".AddCourseActivity"
            android:parentActivityName=".SummaryDetailActivity"
            />
        <activity android:name=".SummaryLVActivity"
            android:launchMode="singleTop">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />

        </intent-filter>
        </activity>

    </application>

</manifest>